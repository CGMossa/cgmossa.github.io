public/

# Local Netlify folder
.netlify

# CV PDF build artefacts (generated by `just cv-pdf`).
# `cv.typ` is tracked so the latest typst output is always inspectable.
.build/
static/cv.pdf

.DS_Store
